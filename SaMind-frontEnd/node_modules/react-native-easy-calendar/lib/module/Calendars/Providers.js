import React from 'react';
import { ThemeContext, LocaleContext } from '../Contexts';
import { DefaultTheme } from '../Themes';
import { DefaultLocale } from '../Locales';
import BaseCalendar from './BaseCalendar';

const Providers = ({
  theme,
  locale,
  ...otherProps
}) => {
  return /*#__PURE__*/React.createElement(LocaleContext.Provider, {
    value: locale !== null && locale !== void 0 ? locale : DefaultLocale
  }, /*#__PURE__*/React.createElement(ThemeContext.Provider, {
    value: theme !== null && theme !== void 0 ? theme : DefaultTheme
  }, /*#__PURE__*/React.createElement(BaseCalendar, otherProps)));
};

export default /*#__PURE__*/React.memo(Providers);
//# sourceMappingURL=Providers.js.map