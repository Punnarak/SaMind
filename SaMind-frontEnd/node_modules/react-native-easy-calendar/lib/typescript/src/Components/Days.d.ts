import React from 'react';
import type { Dayjs } from 'dayjs';
import type { DateProperties, DayComponentType } from '../Entities';
export interface Props {
    DayComponent?: DayComponentType;
    visibleDate: Dayjs;
    minDate?: string;
    maxDate?: string;
    showExtraDates: boolean;
    onPressDay: (date: string) => void;
    dateProperties: {
        [date: string]: DateProperties;
    };
}
declare const Days: React.FC<Props>;
export default Days;
